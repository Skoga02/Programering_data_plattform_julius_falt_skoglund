[project]
name = "programering-data-plattform-julius-falt-skoglund"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi[standard]",
  "kafka-python>=2.3.0",
  "pandas>=3.0.0",
  "psycopg[binary,pool]",
  "python-dotenv",
]
